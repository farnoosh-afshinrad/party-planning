import { styled } from '@mui/material/styles';
import Box from "@mui/material/Box";

export const StyledAppShell = styled(Box)({
    maxWidth:'440px',
    height: '100vh',
    backgroundColor: 'black',
    margin: '0 auto',
    padding: '2rem',
    overflowY:'auto'
})